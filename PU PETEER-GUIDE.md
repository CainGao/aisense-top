# 使用 Puppeteer 实现浏览器自动化

> 更新时间：2026-01-31 20:10
> 方法：Puppeteer + 系统的 Chrome 浏览器
> 自动化：100%，无需用户介入

---

## 🚀 安装和使用

### 安装 Puppeteer
```bash
cd /Users/caingao/aisense-top
npm install puppeteer
```

### 使用 Puppeteer 脚本
```bash
cd /Users/caingao/aisense-top
node scripts/puppeteer-automation.js
```

---

## 📋 操作流程

### 阶段一：登录 GitHub 并创建仓库
1. 访问 GitHub 登录页面
2. 自动输入用户名和密码（需要你提供）
3. 登录成功
4. 访问新建仓库页面
5. 自动填写仓库名称、描述
6. 自动选择公开可见性
7. 自动提交表单
8. 验证仓库创建成功

### 阶段二：推送代码到 GitHub
1. 使用 Git CLI 推送代码
2. 验证推送成功

### 阶段三：登录 Vercel 并链接项目
1. 访问 Vercel 登录页面
2. 自动登录（需要你提供）
3. 链接 GitHub 仓库
4. 验证链接成功

### 阶段四：生产部署
1. 执行 Vercel 生产部署
2. 验证部署成功
3. 获取部署 URL

### 阶段五：配置域名
1. 访问 Vercel Dashboard 域名页面
2. 自动添加域名 aisense.top
3. 获取 DNS 配置信息
4. 自动配置 DNS（如果可能）
5. 验证域名解析

### 阶段六：测试网站访问
1. 测试 Vercel 默认域名访问
2. 测试域名 aisense.top 访问
3. 测试 HTTPS 访问
4. 测试所有页面链接
5. 保存最终截图

---

## 🔧 所需配置

### 环境变量
- `GITHUB_USERNAME`：GitHub 用户名
- `GITHUB_PASSWORD`：GitHub 密码（或 Token）
- `VERCEL_TOKEN`：Vercel Token（可选）

### 配置步骤

#### 步骤1：设置环境变量
```bash
# 在项目根目录创建 .env 文件
cat > /Users/caingao/aisense-top/.env << 'EOF'
GITHUB_USERNAME=CainGao
GITHUB_PASSWORD=your_password
VERCEL_TOKEN=your_token
EOF
```

#### 步骤2：安装依赖
```bash
cd /Users/caingao/aisense-top
npm install puppeteer
```

---

## 🚀 执行自动化

### 立即执行
```bash
cd /Users/caingao/aisense-top
node scripts/puppeteer-automation.js
```

---

## 💡 注意事项

### 安全
- ⚠️ 密码和 Token 不要提交到 Git 仓库
- ⚠️ .env 文件已添加到 .gitignore
- ⚠️ 建议使用 Token 而不是密码

### 浏览器
- ✅ Puppeteer 会使用系统的 Chrome 浏览器
- ✅ 不需要下载额外的浏览器
- ✅ 可以看到浏览器操作（非无头模式）

### 依赖
- Node.js 18+
- npm 9+
- macOS 10.15+（支持 Chrome）
- 系统已安装 Google Chrome

---

## 🎯 完成后

- ✅ GitHub 仓库：https://github.com/CainGao/aisense-top
- ✅ 代码已推送到 GitHub
- ✅ Vercel 部署：https://aisense.vercel.app
- ✅ 域名 aisense.top 已配置
- ✅ 网站可通过 aisense.top 访问
- ✅ 所有页面正常工作

---

**准备开始吗？** 🚀

**设置环境变量后，告诉我："环境变量已设置"**

我会立即执行浏览器自动化！
