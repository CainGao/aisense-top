# GitHub Pages 部署方案

> 更新时间：2026-01-31 20:20
> 目标：域名 aisense.top 直接解析到 GitHub Pages
> 方式：Git CLI（不需要浏览器，不需要 Vercel）

---

## 🚀 方案对比

### ❌ 为什么不用 Vercel？

**Vercel 的问题**：
- ❌ 需要注册 Vercel 账户
- ❌ 需要安装 Vercel CLI
- ❌ 需要配置部署
- ❌ 需要配置域名（在 Vercel Dashboard）
- ❌ 免费计划有限制（Bandwidth、Builds）
- ❌ 需要维护 Vercel 配置

**GitHub Pages 的优势**：
- ✅ **完全免费**：无限 Bandwidth、无限 Builds
- ✅ **不需要安装任何工具**：直接使用 Git CLI
- ✅ **简单配置**：在 GitHub 上启用 Pages
- ✅ **免费 SSL**：HTTPS 证书自动配置
- ✅ **自定义域名**：域名直接解析到 GitHub Pages
- ✅ **持续集成**：Git push 自动触发构建
- ✅ **快速部署**：Git push 后几秒内上线

---

## 📋 GitHub Pages 方案

### 技术原理

#### Git CLI 部署
1. **推送代码到 GitHub**：使用 `git push`
2. **GitHub Pages 自动构建**：GitHub 自动检测到 `gh-pages` 分支
3. **网站自动上线**：构建完成后自动访问

#### 域名解析（DNS 配置）
1. **A 记录**：域名指向 GitHub Pages IP（`185.199.108.153`）
2. **CNAME 记录**：域名指向 GitHub Pages 域名（`*.github.io`）

---

## 🔧 部署步骤

### 阶段一：准备 GitHub 仓库（2 分钟）

#### 步骤1：创建 gh-pages 分支
```bash
cd /Users/caingao/aisense-top
git checkout -b gh-pages
git push -u origin gh-pages
```

#### 步骤2：启用 GitHub Pages
1. **访问**：https://github.com/CainGao/aisense-top/settings/pages
2. **选择 Branch**：`gh-pages`
3. **选择 Folder**：`/ (root)`
4. **点击**："Save"

#### 步骤3：验证 GitHub Pages 已启用
1. **访问**：https://caingao.github.io/aisense-top
2. **应该显示**：AISENSE 网站

---

### 阶段二：配置域名 aisense.top（5 分钟）

#### 选项A：使用 A 记录（推荐）⭐

**你的操作**：
1. **登录域名注册商**（阿里云、腾讯云、Namecheap 等）
2. **找到 DNS 配置**
3. **添加 A 记录**：
   - **主机记录**：`@`（根域名）
   - **记录类型**：`A`
   - **记录值**：`185.199.108.153`（GitHub Pages IP）
   - **TTL**：`600`（或默认）

**验证 DNS**：
```bash
nslookup aisense.top
```

**应该显示**：
```
Server:         185.199.108.153
Address:        185.199.108.153
```

#### 选项B：使用 CNAME 记录（不推荐）

**你的操作**：
1. **登录域名注册商**
2. **添加 CNAME 记录**：
   - **主机记录**：`@`
   - **记录类型**：`CNAME`
   - **记录值**：`caingao.github.io`
   - **TTL**：`600`

**问题**：CNAME 记录不支持根域名，只能使用 `www.aisense.top`

---

### 阶段三：在 GitHub Pages 配置域名（2 分钟）

#### 步骤1：添加自定义域名
1. **访问**：https://github.com/CainGao/aisense-top/settings/pages
2. **点击**："Add a domain"
3. **输入**：`aisense.top`
4. **点击**："Add domain"

#### 步骤2：验证 DNS 配置
- GitHub 会自动检测 DNS 配置
- 如果正确，显示 "DNS check successful"
- 如果错误，显示 DNS 配置问题

#### 步骤3：启用 HTTPS
- HTTPS 证书会自动配置
- 需要等待 10-30 分钟（SSL 证书生成）

---

## 🚀 立即部署

### 我的操作（不需要你的介入）

#### 步骤1：创建 gh-pages 分支
```bash
cd /Users/caingao/aisense-top
git checkout -b gh-pages
git push -u origin gh-pages
```

#### 步骤2：配置 Next.js for GitHub Pages
```bash
# 修改 next.config.mjs
```

#### 步骤3：重新部署
```bash
git add .
git commit -m "Configure for GitHub Pages"
git push
```

### 你的操作（5 分钟）

#### 步骤1：启用 GitHub Pages
1. **访问**：https://github.com/CainGao/aisense-top/settings/pages
2. **选择 Branch**：`gh-pages`
3. **选择 Folder**：`/ (root)`
4. **点击**："Save"

#### 步骤2：配置 DNS（A 记录）
1. **登录域名注册商**
2. **添加 A 记录**：
   - **主机记录**：`@`
   - **记录类型**：`A`
   - **记录值**：`185.199.108.153`
   - **TTL**：`600`

#### 步骤3：在 GitHub Pages 添加域名
1. **访问**：https://github.com/CainGao/aisense-top/settings/pages
2. **点击**："Add a domain"
3. **输入**：`aisense.top`
4. **点击**："Add domain"

---

## 📊 部署时间表

| 操作 | 时间 | 你的时间 |
|------|---------|---------|
| 创建 gh-pages 分支 | 1 分钟 | 0 分钟 |
| 启用 GitHub Pages | 2 分钟 | **2 分钟** |
| 配置 DNS（A 记录） | 3 分钟 | **3 分钟** |
| GitHub Pages 添加域名 | 2 分钟 | 0 分钟 |
| DNS 传播 | 10-60 分钟 | 0 分钟 |
| **总计** | **~78 分钟** | **5 分钟** |

---

## 💡 常见问题

### Q1：为什么不用 Vercel？
**A**：GitHub Pages 更简单：
- 完全免费（无限制）
- 不需要安装任何工具
- 域名直接解析到 GitHub
- Git push 自动部署
- HTTPS 证书自动配置

### Q2：GitHub Pages 支持哪些框架？
**A**：支持所有静态网站框架：
- Next.js（使用 `output: 'export'`）
- React
- Vue
- Angular
- Svelte
- 纯静态网站

### Q3：GitHub Pages 有限制吗？
**A**：限制很少：
- ✅ 无限 Bandwidth
- ✅ 无限 Builds
- ✅ 免费自定义域名
- ✅ 免费 HTTPS
- ❌ 无服务器端代码（只能静态网站）
- ❌ 无数据库

### Q4：如何更新网站？
**A**：很简单：
```bash
git add .
git commit -m "Update website"
git push origin gh-pages
```

GitHub Pages 会自动构建和部署！

---

## 🎯 总结

### 方案
- ✅ **GitHub Pages**：完全免费，域名直接解析，Git push 自动部署
- ❌ **Vercel**：需要注册、配置、维护，免费版有限制

### 你的操作（5 分钟）
1. 启用 GitHub Pages（2 分钟）
2. 配置 DNS（A 记录，3 分钟）

### 我的操作（1 分钟）
1. 创建 gh-pages 分支
2. 配置 Next.js for GitHub Pages
3. 重新部署

### 最终结果
- ✅ 域名 aisense.top 直接解析到 GitHub Pages
- ✅ Git push 自动部署
- ✅ HTTPS 证书自动配置
- ✅ 完全免费
- ✅ 无需 Vercel

---

## 🚀 立即行动

### 你的操作（5 分钟）

#### 步骤1：启用 GitHub Pages
1. **访问**：https://github.com/CainGao/aisense-top/settings/pages
2. **选择 Branch**：`gh-pages`
3. **选择 Folder**：`/ (root)`
4. **点击**："Save"

#### 步骤2：配置 DNS（A 记录）
1. **登录域名注册商**
2. **添加 A 记录**：
   - **主机记录**：`@`
   - **记录类型**：`A`
   - **记录值**：`185.199.108.153`
   - **TTL**：`600`

**完成后告诉我**："GitHub Pages 已启用"

---

## 💡 我的承诺

### 如果 GitHub Pages 已启用

我会立即：
1. ✅ 创建 gh-pages 分支
2. ✅ 配置 Next.js for GitHub Pages
3. ✅ 推送代码到 GitHub
4. ✅ 在 GitHub Pages 添加域名 aisense.top
5. ✅ 提供最终访问 URL

**预计完成时间**：3-5 分钟

---

**准备好了吗？** 🚀

**现在就启用 GitHub Pages，然后告诉我："GitHub Pages 已启用"**

**我会立即完成所有配置！**
